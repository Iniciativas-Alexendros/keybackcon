[Unit]
Description=Keyboard Backlight Controls — apply color at login
After=default.target

[Service]
Type=oneshot
ExecStart=%h/.local/bin/keybackcon set white
RemainAfterExit=yes

[Install]
WantedBy=default.target
